import React, { memo } from 'react';
// import Page from '../Page';
import { Container } from './style';
// import {} from './interfaces';

const Dialogs: React.FC = memo(() => {
    return (
        // <Page>
        <Container>Dialogs</Container>
        // </Page>
    );
});
export default Dialogs;
